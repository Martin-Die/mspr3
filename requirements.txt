# ─────────────────────────────────────────────────────────────────────────────
# EDF MSPR Bloc 3 — Dépendances Python
# ─────────────────────────────────────────────────────────────────────────────

# API
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.29.0,<1.0.0
pydantic>=2.7.0,<3.0.0

# ML
scikit-learn>=1.4.0,<2.0.0
joblib>=1.4.0,<2.0.0
numpy>=1.26.0,<2.0.0
pandas>=2.2.0,<3.0.0

# MLflow — tracking + model registry
mlflow>=2.13.0,<3.0.0

# Data sources
openpyxl>=3.1.0          # lecture fichiers .xls / .xlsx RTE

# Snowflake
snowflake-connector-python>=3.9.0
snowflake-sqlalchemy>=1.5.0

# Observabilité
prometheus-client>=0.20.0

# Load testing (dev/CI uniquement)
locust>=2.28.0

# Tests
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0             # client async pour TestClient FastAPI